{
  "name": "quotes",
  "version": "1.0.0",
  "description": "Share quotes",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/netlify-lambda serve /src",
    "build": "./node_modules/.bin/netlify-lambda build /src",
    "server": "nodemon server.js",
    "both": "concurrently \"nodemon server_dev.js\" \"cd client && npm start\""
  },
  "author": "Hassan",
  "license": "UNLICENSED",
  "dependencies": {
    "concurrently": "^4.1.0",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "mongoose": "^5.5.7",
    "netlify-lambda": "^1.4.11",
    "react-router-dom": "^5.0.0",
    "serverless-http": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.0"
  }
}
